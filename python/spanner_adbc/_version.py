# Kept in sync with the crate version in Cargo.toml by cargo-release (see
# pre-release-replacements there). CI overwrites this from the pushed vX.Y.Z
# tag when building release wheels.
__version__ = "0.7.0"
