// Spanner override: mandatory PRIMARY KEY, native type name, and an explicit INSERT column list.

// part: setup_query

CREATE TABLE test_string (
    res STRING(MAX)
) PRIMARY KEY (res)

// part: bind_query

INSERT INTO test_string (res) VALUES ($1)
