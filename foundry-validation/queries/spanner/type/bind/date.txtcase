// Spanner override: mandatory PRIMARY KEY, native type name, and an explicit INSERT column list.

// part: setup_query

CREATE TABLE test_date (
    res DATE
) PRIMARY KEY (res)

// part: bind_query

INSERT INTO test_date (res) VALUES ($1)
