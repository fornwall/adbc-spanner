// Spanner override: FLOAT32 cannot be a primary key, so declare no key at all (Spanner keys
// such a table on a hidden `rowid` of its own) and keep res a plain column.

// part: setup_query

CREATE TABLE test_float32 (
    res FLOAT32
)

// part: bind_query

INSERT INTO test_float32 (res) VALUES ($1)
